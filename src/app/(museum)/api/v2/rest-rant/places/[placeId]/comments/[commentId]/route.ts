/**
 * /api/v2/rest-rant/places/[placeId]/comments/[commentId] — Enhanced tier.
 *
 * Re-exports v1's DELETE handler. tier="enhanced" recorded via tierFromUrl().
 */

export { DELETE } from "../../../../../../rest-rant/places/[placeId]/comments/[commentId]/route";
