/**
 * /api/v2/rest-rant/places/[placeId]/comments — Enhanced tier.
 *
 * Re-exports v1's POST handler. tier="enhanced" recorded via tierFromUrl().
 */

export { POST } from "../../../../../rest-rant/places/[placeId]/comments/route";
